import React from "react";
import AssignDeductions from "@/components/app-modules/AssignDeductions";

export const metadata = {
  title: "Assign Deductions - API HRM",
};

const Page = () => {
  return <AssignDeductions />;
};

export default Page;
