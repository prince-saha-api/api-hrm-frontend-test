import React from "react";
import Notices from "@/components/app-modules/Notices";

export const metadata = {
  title: "Notices - API HRM",
};

const Page = () => {
  return <Notices />;
};

export default Page;
