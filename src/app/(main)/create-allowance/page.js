import React from "react";
import CreateAllowance from "@/components/app-modules/CreateAllowance";

export const metadata = {
  title: "Allowance - API HRM",
};

const Page = () => {
  return <CreateAllowance />;
};

export default Page;
