import React from "react";
import RemoteAttendance from "@/components/app-modules/RemoteAttendance";

export const metadata = {
  title: "Remote Attendance - API HRM",
};

const Page = () => {
  return <RemoteAttendance />;
};

export default Page;
