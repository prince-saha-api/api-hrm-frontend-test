import React from "react";
import UserRole from "@/components/app-modules/UserRole";

export const metadata = {
  title: "User Roles - API HRM",
};

const Page = () => {
  return <UserRole />;
};

export default Page;
