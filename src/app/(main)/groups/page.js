import React from "react";
import Groups from "@/components/app-modules/Groups";

export const metadata = {
  title: "Groups - API HRM",
};

const Page = () => {
  return <Groups />;
};

export default Page;
