import React from "react";
import Devices from "@/components/app-modules/Devices";

export const metadata = {
  title: "Devices - API HRM",
};

const Page = () => {
  return <Devices />;
};

export default Page;
