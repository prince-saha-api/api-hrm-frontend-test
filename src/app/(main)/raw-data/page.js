import React from "react";
import RawData from "@/components/app-modules/RawData";

export const metadata = {
  title: "Raw Data - API HRM",
};

const Page = () => {
  return <RawData />;
};

export default Page;
