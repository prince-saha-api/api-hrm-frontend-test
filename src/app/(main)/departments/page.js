import React from "react";
import Departments from "@/components/app-modules/Departments";

export const metadata = {
  title: "Departments - API HRM",
};

const Page = () => {
  return <Departments />;
};

export default Page;
