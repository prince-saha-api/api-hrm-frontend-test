import React from "react";
import BasicInfo from "@/components/app-modules/BasicInfo";

export const metadata = {
  title: "Basic Info - API HRM",
};

const Page = () => {
  return <BasicInfo />;
};

export default Page;
